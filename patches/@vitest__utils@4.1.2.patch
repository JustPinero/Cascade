diff --git a/dist/source-map/node.js b/dist/source-map/node.js
index b1e322a10c7a47f3c7a5d0e72d04a80a3be705e1..6d2ade0424ecd11eb688d4f19bad2d3c141ce498 100644
--- a/dist/source-map/node.js
+++ b/dist/source-map/node.js
@@ -5,8 +5,21 @@ import convertSourceMap from 'convert-source-map';
 // based on vite
 // https://github.com/vitejs/vite/blob/84079a84ad94de4c1ef4f1bdb2ab448ff2c01196/packages/vite/src/node/server/sourcemap.ts#L149
 function extractSourcemapFromFile(code, filePath) {
-	const map = (convertSourceMap.fromSource(code) || convertSourceMap.fromMapFileSource(code, createConvertSourceMapReadMap(filePath)))?.toObject();
-	return map ? { map } : undefined;
+	// Cascade Phase 30 — `convert-source-map`'s comment regex matches
+	// literal "sourceMappingURL=data:application/json;base64," strings
+	// inside bundled tools that GENERATE sourcemap comments (e.g. tsx's
+	// register.cjs). The base64 content after that literal is JS source,
+	// not a real inline map, so JSON.parse throws. When vitest tries to
+	// symbolicate a stack frame pointing into such a file, this throws
+	// up as an "unhandled error" that breaks the test runner exit code.
+	// Swallow the error; missing one source-map for a vendored bundle
+	// is harmless, but breaking the runner exit code is not.
+	try {
+		const map = (convertSourceMap.fromSource(code) || convertSourceMap.fromMapFileSource(code, createConvertSourceMapReadMap(filePath)))?.toObject();
+		return map ? { map } : undefined;
+	} catch {
+		return undefined;
+	}
 }
 function createConvertSourceMapReadMap(originalFileName) {
 	return (filename) => {
