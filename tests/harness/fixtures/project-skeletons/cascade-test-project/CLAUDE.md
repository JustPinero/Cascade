# Cascade Test Project

A minimal project skeleton used as the default path for `rig.createProject(...)` when no `path` is provided. The contents of this directory only matter to the dispatcher's readiness check.
