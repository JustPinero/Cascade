# Request 8.5 — Public Release Polish

## Objective
Final cleanup for public release. Remove hardcoded references to
Justin's projects, clean up debug artifacts, ensure first-run
experience is smooth for a new user.

## Acceptance Criteria → Test Mapping

| Criterion | Test |
|-----------|------|
| No hardcoded project paths in source code | grep finds no /Users/justinpinero in *.ts files |
| PROJECTS_DIR defaults to ~/projects if not set | resolveProjectsDir returns ~/projects as default |
| First scan with empty DB shows helpful empty state | dashboard renders "Scan to import projects" message |
| New user sees Overseer (not Delamain) by default | default overseer name is "Overseer" |
| Settings page works with zero configuration | settings page renders all sections without errors |
| README quick-start works end-to-end | README steps produce a running Cascade instance |
| No personal knowledge lessons in committed files | knowledge/ contains only templates and deployment-playbook |
| Portrait defaults work without custom upload | default overseer portrait renders |

## RED Phase
- grep source for hardcoded user paths returns empty
- resolveProjectsDir returns sensible default on fresh install
- overseer settings default to "Overseer" name

## GREEN Phase
- Audit all source files for hardcoded paths
- Update lib/validators.ts resolveProjectsDir default
- Create first-run empty states for dashboard, knowledge, tasks
- Verify default portrait and name render correctly
- Final README review
- Clean up any debug console.logs or TODO comments

## Files to Touch
- lib/validators.ts
- app/page.tsx (empty state)
- app/knowledge/page.tsx (empty state)
- app/tasks/page.tsx (empty state)
- README.md (final review)

## Dependencies
- 8.1 (customizable overseer)
- 8.3 (gitignore personal data)
- 8.4 (Windows support in README)
