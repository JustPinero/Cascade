# Request 8.3 — Gitignore Personal Data for Public Release

## Objective
Ensure personal data (playbook rules, knowledge lessons, session logs,
channel messages, database) is never committed. Ship with empty
templates that users populate with their own content.

## Acceptance Criteria → Test Mapping

| Criterion | Test |
|-----------|------|
| knowledge/overseer-playbook.md is gitignored | .gitignore contains overseer-playbook.md |
| knowledge/*.md lesson files are gitignored | .gitignore contains knowledge/*.md |
| .claude/sessions/ is gitignored | .gitignore contains .claude/sessions/ |
| .claude/engineer-channel.md is gitignored | .gitignore contains engineer-channel.md |
| .claude/kilroy-channel.md is gitignored | .gitignore contains kilroy-channel.md |
| dev.db is gitignored | .gitignore contains dev.db |
| Template playbook ships with examples | overseer-playbook.example.md exists with commented guidance |
| knowledge/deployment-playbook/ stays tracked | deployment-playbook is NOT gitignored (generic, not personal) |

## RED Phase
- Verify .gitignore entries exist for each personal data path
- Verify example template files exist

## GREEN Phase
- Update .gitignore with personal data exclusions
- Create knowledge/overseer-playbook.example.md with commented template
- Move current playbook content to local-only (already in .gitignore)
- Verify deployment-playbook/ and deployment landmines are still tracked
- Run git status to verify nothing personal shows up

## Files to Touch
- .gitignore
- knowledge/overseer-playbook.example.md (new)

## Dependencies
- None
