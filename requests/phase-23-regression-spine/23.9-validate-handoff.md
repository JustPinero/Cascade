# Request 23.9 — Validate + Handoff

## Objective

Final smoke + bookkeeping. After this slice, Phase 23 is "definition of done" complete: regression spine in place, caching live and measurable, evals and scenario tests gating CI, no half-finished work.

## Acceptance Criteria → Tests (write these FIRST)

| # | Criterion | Verification |
|---|-----------|--------------|
| 1 | `scripts/validate.sh` exits 0 — runs lint, typecheck, prisma generate, vitest, build | `pnpm validate` |
| 2 | `pnpm eval` exits 0 with all scenarios passing | `pnpm eval` |
| 3 | `pnpm test:e2e` exits 0 with the 3 smoke tests passing | `pnpm test:e2e` |
| 4 | `tests/scenarios/` all pass | `pnpm test tests/scenarios/` |
| 5 | `lib/__tests__/prompt-snapshots.test.ts` passes | `pnpm test lib/__tests__/prompt-snapshots.test.ts` |
| 6 | `/observability/cache` page renders with at least one row from a real production turn after this branch is deployed | manual — note in handoff.md |
| 7 | The Overseer turn 2 in a fresh session shows `cacheReadInputTokens > 0` in `/observability/cache` | manual — note in handoff.md |
| 8 | `audits/debt.md` Theme Pack reference is moved from "Phase 23" to a later phase number (Phase 26 or whichever lands next in the user's queue) | grep |
| 9 | `audits/debt.md` lists the deferred items from this phase: streaming usage logging (wizard + project chat), 5–10 sanitized real session logs in the escalation corpus, `resultUsed` heuristic for tool-call observability (after 24.2 ships) | grep |
| 10 | `.claude/handoff.md` updated with a Phase 23 summary | grep |
| 11 | No new entries in `audits/debt.md` introduced silently — every deferral above is documented and intentional | review |

## Steps

This slice is checklist work. There are no production-code changes beyond bookkeeping files.

- [ ] Run `pnpm validate`. Resolve any failures.
- [ ] Run `pnpm eval` with the 23.7 fixtures. Confirm all pass.
- [ ] Run `pnpm test:e2e` locally. Confirm 3 smokes pass.
- [ ] Run `pnpm test tests/scenarios/`. Confirm all pass.
- [ ] Edit `audits/debt.md`:
  - Move the Theme Pack note to its new phase home.
  - Add a "Phase 23 deferred" section listing:
    - **Streaming usage logging** for `/api/wizard/chat` and `/api/projects/[slug]/chat`. Pattern lands with 25.2; until then, those call sites' cache hits are invisible in `/observability/cache` even though caching itself is working.
    - **Real-world escalation logs.** The corpus is synthetic. A future slice can add 5-10 sanitized real logs from the user's fleet to catch patterns the synthetic corpus misses.
    - **`resultUsed` heuristic** in `ToolCallEvent` (the column reservation lands in 24.2; population logic deferred until basic data is flowing for at least a week).
    - **`runDispatchWatchdog` scheduling.** The function is callable; tests invoke it directly. Wire it into a Next.js route or scheduled job — small slice, can land any time after 23.2.
    - **Legacy webhook fallback removal.** After production telemetry shows zero orphaned-webhook events for a sustained window, remove the legacy `session-launched` activity-event lookup from `app/api/webhook/session-complete/route.ts`.
    - **Charts and aggregations on `/observability/cache`** — demand-driven; promote when patterns recur.
- [ ] Edit `.claude/handoff.md` with a Phase 23 summary:
  - Rig in place; scenario tests catching the dispatch races
  - Caching live with measured hit rates from `/observability/cache`
  - Eval runner online with N fixtures (Overseer 5, knowledge-matcher 3, escalation-detector 30+)
  - E2E re-enabled in CI
  - Note the manual verification results from criteria 6-7 (post-deploy hit rate observation)
  - State the next phase: Phase 24 — Overseer Intelligence
- [ ] After merge to main and one production turn, capture the `/observability/cache` cache hit rate and append to handoff.md as evidence the caching landed correctly. If hit rate is below 50% on the second Overseer call, log it as a follow-up.

## Dependencies

- All previous Phase 23 slices: 23.1, 23.2, 23.3, 23.4, 23.5, 23.6, 23.7, 23.8.

## Out of scope

- **Production deployment.** This slice ends at "branch is ready to merge." Merge and deploy are user actions.
- **Refresh of recordings against the live API.** If 23.7's recordings need updating, that's a separate `pnpm eval:refresh` invocation outside the validate flow.
- **Performance benchmarking of the new infrastructure.** Validation here is correctness — does the suite pass — not "did we get faster."

## Definition of done

- All 11 criteria green.
- handoff.md updated.
- audits/debt.md updated.
- Phase 23 branch is mergeable.
