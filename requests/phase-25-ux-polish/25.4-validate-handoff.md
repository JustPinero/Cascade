# Request 25.4 — Validate + Handoff

## Objective

Final smoke + bookkeeping for Phase 25. Adaptive thinking, streaming, and inline citations are all live; this slice confirms each is working in real traffic and captures the evidence in handoff.md.

## Acceptance Criteria → Tests (write these FIRST)

| # | Criterion | Verification |
|---|-----------|--------------|
| 1 | `scripts/validate.sh` exits 0 | `pnpm validate` |
| 2 | `pnpm eval` exits 0 with all scenarios from earlier phases plus the 25.1 + 25.3 additions | `pnpm eval` |
| 3 | `pnpm test:e2e` exits 0 | `pnpm test:e2e` |
| 4 | Manual: open Overseer chat, send a multi-turn dispatch decision, observe text streaming token-by-token. TTFT (time to first token) is under 1 second on a cache-warm conversation | manual — note in handoff.md |
| 5 | Manual: ask the Overseer a knowledge-bearing question; observe inline `[L-N]` superscript citation links in the rendered response; click one and arrive at the lesson detail page | manual — note in handoff.md |
| 6 | Manual: run a dispatch decision turn; verify in `/observability/tools` that `query_outcome_history` (24.1) and `query_knowledge_with_citations` (25.3) both appear when relevant | manual — note in handoff.md |
| 7 | `/observability/cache` shows non-zero `cacheReadInputTokens` after a multi-turn session — proves caching survived the streaming + thinking + citations changes | manual — note in handoff.md |
| 8 | The 23.3 follow-up (streaming usage logging for wizard + project chat) is removed from `audits/debt.md` — closed by 25.2 | grep |
| 9 | `audits/debt.md` adds Phase 25 deferred items: Files API adoption (post knowledge-matcher v2), citation feedback feature, manual extended thinking on Sonnet 4.5 (only if adaptive proves insufficient) | grep |
| 10 | `.claude/handoff.md` updated with Phase 25 summary | grep |

## Steps

- [ ] Run `pnpm validate`, `pnpm eval`, `pnpm test:e2e`.
- [ ] Open `/delamain` and run a 5-turn conversation that exercises:
  - dispatch proposal (tests 25.1 thinking + 24.1 outcome history)
  - knowledge-bearing question (tests 25.3 citations)
  - long-running session that triggers history compression (tests caching across compressed history per 23.4)
- [ ] Use browser dev tools to measure TTFT for the second turn onward; confirm it's under 1 second on cache-warm prompts.
- [ ] Click an inline citation; verify the lesson detail page loads.
- [ ] Open `/observability/tools` and `/observability/cache`. Verify both have rows; verify cache hit rate is meaningful (>50% on second turn onward).
- [ ] Edit `audits/debt.md`:
  - Remove the streaming-usage-logging item (closed by 25.2).
  - Add **Files API adoption** — research-only doc exists at `references/anthropic-files-api.md`. Adopt when knowledge-matcher v2 lands or corpus grows past ~50K tokens regularly attached.
  - Add **Citation feedback / correction feature** — "this citation was wrong" UI. Future.
  - Add **Manual extended thinking on Sonnet 4.5 for dispatch decisions** — only if adaptive thinking on Sonnet 4.6 doesn't measurably improve proposal quality. Adds a model split; defer unless needed.
- [ ] Edit `.claude/handoff.md` with Phase 25 summary:
  - Adaptive thinking flowing; thinking blocks correctly preserved across tool turns
  - Streaming live on Overseer chat; TTFT measurable; usage logging working for all three streaming endpoints (overseer, wizard, project chat)
  - Inline citations rendering; harvest loop's value now visible
  - Cache hit rate stable through the streaming + thinking + citation changes
  - Mention any unexpected findings (e.g., a tool that fires more often than expected, a citation pattern that surprises)
  - State next phase based on the user's roadmap

## Dependencies

- All previous Phase 25 slices: 25.1, 25.2, 25.3.

## Out of scope

- **Production deployment.**
- **Re-recording evals against live API** unless prompts changed.
- **Performance benchmarking beyond the simple TTFT smoke.** Real perf testing is a future slice if needed.

## Definition of done

- All 10 criteria green.
- handoff.md reflects Phase 25 state with the manual evidence captured.
- Phase 25 branch is mergeable.
- Three phase-23-through-25 worth of regression spine, observability, AI intelligence, and UX polish all shipped.
