# Request 41.4 — Fleet Reconciliation in the Health Engine

## Objective
Cascade's DB picture of the fleet must survive contact with reality.
Observed drift on 2026-07-07: a Project row pointing at a path that no
longer exists (KickoffPrompts), a "complete" project with 1,246 dirty
files (labwebsite), path-casing mismatches (Desktop/Projects vs
Desktop/projects), and a repo whose local main was 4 phases behind
origin while another machine pushed (near-miss during the migration).

## Acceptance Criteria → Test Mapping

| Criterion | Test |
|-----------|------|
| Missing project path → reconciliation finding | fixture: Project row with dead path → finding `path-missing` |
| Path casing normalized before comparison | fixture: row with Projects/ vs disk projects/ on case-insensitive FS → no false `path-missing`, but a `path-casing` notice |
| Dirty-tree count reported | scratch repo with N dirty files → finding includes count |
| Ahead/behind origin computed | scratch repo with local commits ahead + behind a fixture remote → both counts reported |
| Unpushed branches detected | scratch repo with a local-only branch → finding lists it |
| Status contradiction flagged | Project.status='complete' + dirty tree/unpushed work → `status-drift` finding |
| No remote → graceful | repo without origin → no crash, remote checks skipped with reason |
| Morning briefing includes reconciliation drift | briefing payload contains a drift section when findings exist |

## RED Phase
All eight rows as failing tests against scratch git fixtures (never
the live fleet, never dev.db).

## GREEN Phase
Goal condition: all 41.4 RED tests pass AND scripts/validate.sh exits 0.

## Files to Touch (verify at Prime)
- lib/health-engine.ts (or a new lib/fleet-reconciler.ts it calls)
- morning-briefing module (locate at Prime)
- fleet dashboard surface for drift (minimal — a count + list)
- references/architecture.md

## Constraints
- Read-only against project repos; `git fetch` is allowed but must be
  timeboxed (AbortController/timeout) and failure-tolerant (offline =
  skip remote checks with reason, not an error).
- Shell out with arg arrays, never interpolated strings
  (security-guidance: no user/DB input into shell strings).

## Dependencies
41.1. Provides the plumbing 41.7 builds on.
