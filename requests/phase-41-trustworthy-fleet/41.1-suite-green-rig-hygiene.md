# Request 41.1 — Suite Green + Test-Rig Hygiene

## Objective
Make the test suite honest before the phase builds on it: fix the 3
pre-existing failures and stop the dispatch test rig from leaking
scratch databases (568 `test-rig-*.db` files had accumulated in
`prisma/` before a manual cleanup on 2026-07-07).

## Context
- `lib/github.test.ts` and `lib/retroactive-harvester.test.ts` fail by
  timeout while shelling out to `git`/`gh` — they fail identically on a
  clean checkout, so this is environmental coupling, not a regression.
- The dispatch rig (`tests/harness/dispatch-rig.ts`) has dispose
  cleanup, but files still accumulate — likely crashed/interrupted runs
  never reach dispose.

## Acceptance Criteria → Test Mapping

| Criterion | Test |
|-----------|------|
| github tests pass without network/gh auth | existing tests pass with shell-outs mocked or guarded (mock the boundary — no real `gh` calls in unit tests, per .claude/rules/tests.md) |
| retroactive-harvester tests pass deterministically | existing tests pass with git shell-outs against a scratch fixture repo or mocked |
| rig scratch DBs are cleaned up even after interrupted runs | new test: rig setup removes stale `test-rig-*.db` files older than the current run |
| validate.sh reflects a fully green suite | `pnpm test` exits 0 with no skips added to hide failures |

## RED Phase (write these tests FIRST)
- rig startup sweeps stale `test-rig-*.db` files (create a fake stale
  file, boot the rig, assert it's gone)
- Reproduce the two timeout failures locally, then convert their
  external dependencies to mocks/fixtures so they fail FAST (assertion,
  not timeout) before the fix

## GREEN Phase
Goal condition: `pnpm test` exits 0, all files, zero pre-existing
failures, plus the new rig-hygiene test passes.

## Files to Touch (verify at Prime)
- lib/github.test.ts, lib/retroactive-harvester.test.ts
- tests/harness/dispatch-rig.ts
- possibly lib/github.ts / lib/retroactive-harvester.ts (injectable
  shell boundary only — no behavior change)

## Constraints
- Do NOT weaken assertions or skip tests to get green.
- No `any`, no `@ts-ignore` (strict mode applies to tests).

## Dependencies
None — this unblocks the rest of the phase.
