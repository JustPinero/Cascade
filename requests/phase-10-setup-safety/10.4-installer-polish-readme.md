# Request 10.4 — Installer Polish, README, Focus Test

## Objective
Portfolio-grade first impression. Rewrite Cascade's README around
`npx create-cascade`. Produce a troubleshooting guide. Run focus
tests with two early testers (one Mac, one PC); record friction and
feed fixes back into the installer.

## Acceptance Criteria → Test Mapping

| Criterion | Test |
|-----------|------|
| README.md has `npx create-cascade` as the primary install path | content grep: `npx create-cascade` appears in a prominent quick-start section |
| README has "Windows (WSL2)", "macOS", "Linux" prereq sections | section header grep |
| README states 1P as required and explains rationale briefly | content grep for "1Password" + rationale keywords |
| README links to `docs/troubleshooting.md` | link presence |
| `docs/troubleshooting.md` covers: op signin, WSL memory config, hook misconfig, port conflicts, prereq install on each OS | section header grep |
| Focus test: Mac install; friction logged | `audits/install-feedback.md` contains a Mac entry |
| Focus test: Windows install; friction logged | `audits/install-feedback.md` contains a Windows entry |
| All friction items from focus test either fixed or logged to `audits/debt.md` with justification | audit file review |

## RED Phase
- Failing content test on README.md (expect `npx create-cascade` present; currently absent)
- Failing content test on `docs/troubleshooting.md` (file doesn't exist yet)
- Failing audit test: `audits/install-feedback.md` doesn't exist
Run `pnpm test` (adjust to validate-style check if content tests aren't in vitest) → fail.

## GREEN Phase
- Rewrite README.md:
  - Quick start with `npx create-cascade`
  - Per-OS prereq sections
  - 1P rationale + cost disclosure
  - Screenshots of the Overseer dashboard, dispatch view
  - Link to troubleshooting
- Write `docs/troubleshooting.md` with common issues and fixes
- Run focus tests — one Mac install, one Windows install, plus a fresh VM if available
- Log friction to `audits/install-feedback.md`
- Fix critical friction directly in 10.3; log non-blocking to `audits/debt.md`
- Final validation: `scripts/validate.sh` green, README renders cleanly on GitHub

## Files to Touch
- `README.md` (rewrite)
- `docs/troubleshooting.md` (new)
- `audits/install-feedback.md` (new)
- `audits/debt.md` (update if deferred items found)
- Any installer fixes loop back to create-cascade repo

## Dependencies
- 10.1, 10.2, 10.3 must all be green before focus test begins

## Notes
- Don't overdesign the README. Keep it skimmable: what, why, install, first-run, features summary, link to docs.
- Focus test is human-in-the-loop. Plan a 1-hour slot with each tester, observe via screen share, don't help unless they're stuck >5 min.
- Friction metric: number of times the user had to read external docs or ask a question to proceed. Target: zero for the happy path.
