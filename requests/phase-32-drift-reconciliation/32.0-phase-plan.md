# Phase 32 — Drift reconciliation

Closes audit finding **[30.D3]**. Documentation-only — four reference files are stale or incomplete.

## Scope

- **`references/schema.md`** — 4 missing models (`Dispatch`, `FeatureProposal`, `ToolCallEvent`, `AnthropicUsageEvent`), 5 missing `Project` fields, 5 missing relations.
- **`references/api-contracts.md`** — 24 of 41 routes undocumented; method + request/response shape missing for most.
- **`references/env-vars.md`** — 5 vars used in code but not documented.
- **`references/architecture.md`** — Phases 28/29/30 not reflected.

## Approach

Mechanical doc work. Each file gets a focused update against the audit's enumeration. No code changes, no test changes.

The api-contracts rewrite is the largest — 41 routes × method/request/response. Delegate to a general-purpose agent that walks `app/api/**/route.ts` and emits the doc; verify the result.

## Acceptance criteria

| # | Criterion | Verification |
|---|-----------|--------------|
| 1 | `schema.md` includes every Prisma model — checked by enumerating `^model ` in the schema and matching `## ` headings in the doc | manual diff |
| 2 | `api-contracts.md` includes every route under `app/api/**/route.ts` | manual diff |
| 3 | `env-vars.md` includes every var found via `grep "process.env\."` (filtered to unique names) | manual diff |
| 4 | `architecture.md` mentions preflight UI (Phase 28), wt split-pane (Phase 29), and the `@vitest/utils` patch (Phase 30) | manual review |
| 5 | `pnpm test` still 990/0/6 (no test changes, but verify) | full suite |
| 6 | `pnpm exec tsc --noEmit` clean (sanity check) | typecheck |

## Out of scope

- Restructuring the doc surface — keep current layout.
- New diagrams or expanded prose beyond what's needed to close drift.
- Test coverage for routes — Phase 33.
