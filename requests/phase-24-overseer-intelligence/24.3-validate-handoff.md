# Request 24.3 — Validate + Handoff

## Objective

Final smoke + bookkeeping for Phase 24. Outcome-conditioned dispatch and tool-call observability are both live; this slice confirms they're working in production traffic and updates the running documentation.

## Acceptance Criteria → Tests (write these FIRST)

| # | Criterion | Verification |
|---|-----------|--------------|
| 1 | `scripts/validate.sh` exits 0 | `pnpm validate` |
| 2 | `pnpm eval` exits 0 (Phase 23 fixtures + the 4 new ones from 24.1 and 24.2) | `pnpm eval` |
| 3 | `pnpm test:e2e` exits 0 | `pnpm test:e2e` |
| 4 | One real Overseer turn proposes a dispatch on a project with audit-heavy history; the proposal references the audit pattern | manual — note in handoff.md |
| 5 | After running an Overseer chat session, `/observability/tools` shows the expected ToolCallEvent rows | manual — note in handoff.md |
| 6 | The Overseer answers "which tools do you call most this week?" by invoking `get_tool_call_stats` (visible in `ToolCallEvent` itself) | manual |
| 7 | `audits/debt.md` adds Phase 24 deferred items: `resultUsed` heuristic population, cross-session correlation queries, alerting on tool failure rate | grep |
| 8 | `.claude/handoff.md` updated with Phase 24 summary | grep |

## Steps

- [ ] Run `pnpm validate`, `pnpm eval`, `pnpm test:e2e`.
- [ ] Open `/delamain` and ask "is anything stalled lately?" Inspect the proposal text — verify the Overseer surfaces outcome history in its reasoning when relevant.
- [ ] Open `/observability/tools` after the chat — verify rows appear with the expected fields.
- [ ] Edit `audits/debt.md`:
  - Add **`resultUsed` heuristic population** — heuristic logic to detect whether a tool's output was referenced in the next assistant turn. Schema column reserved in 24.2; population deferred until at least a week of basic data is flowing.
  - Add **Cross-session correlation queries** — e.g. "tools whose output rarely gets used across sessions." Phase 26+ if proven useful.
  - Add **Alerting on tool failure rate spikes** — future, data lands now via 24.2.
- [ ] Edit `.claude/handoff.md` with Phase 24 summary:
  - `query_outcome_history` tool live; system prompt updated; 3 evals passing
  - `ToolCallEvent` instrumentation flowing; `/observability/tools` populated
  - `get_tool_call_stats` answering questions in chat
  - Sample queries the Overseer can now answer about itself, copied verbatim from a real chat turn
  - Next phase: Phase 25 — UX Polish (adaptive thinking, streaming, citations)

## Dependencies

- All previous Phase 24 slices: 24.1, 24.2.

## Out of scope

- **Production deployment.**
- **Re-recording of evals** unless the prompt changes.

## Definition of done

- All 8 criteria green.
- handoff.md reflects Phase 24 state.
- Phase 24 branch is mergeable.
