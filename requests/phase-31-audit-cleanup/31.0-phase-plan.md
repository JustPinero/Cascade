# Phase 31 — Security + perf + cleanup

Closes the CRITICAL finding and four MEDIUM/HIGH findings from the 2026-06-09 audit. Single focused commit. All fixes XS-S effort.

## What ships

### Security
- **[30.D1]** `lib/claude-dispatcher.ts:454` — replace single-quote strip with `sanitizeForShell(projectName)`. Closes the shell-injection vector via project name.

### Perf — Prisma indexes on hot paths
- **[30.D4]** Add `@@index` to:
  - `Project.lastActivityAt` (dashboard, briefing, query-projects sort)
  - `ActivityEvent.createdAt` (polling activity feed)
  - `ActivityEvent.(projectId, createdAt)` (per-project activity)
  - `HumanTask.status` (briefing + /api/tasks filter)
  - `ChatMessage.(sessionDate, createdAt)` (overseer history fetch)
- `pnpm exec prisma db push` to materialize. Schema is the change; ORM behavior is unchanged.

### Defensive cleanup
- **[30.D5]** `lib/rate-limiter.ts` — add a periodic sweeper that drops expired entries. Probabilistic (every Nth call) so it's free at low traffic and bounded at high traffic.
- **[30.D6]** Wrap three `JSON.parse` calls in try/catch with safe fallback:
  - `app/knowledge/page.tsx:197` and `app/knowledge/[category]/page.tsx:79` (lesson tags → `[]`)
  - `lib/anthropic-feature-check.ts:219` (LLM output → return-early with empty/no-detection result)
- **[30.D7]** Add `AbortController` + timeout to three fetches:
  - `lib/deploy-monitor.ts` Vercel + Railway (10s)
  - `app/components/overseer-chat.tsx:462` client-side overseer chat (90s, matches server-side 60s contract + margin)

## Acceptance criteria → tests

| # | Criterion | Test |
|---|-----------|------|
| 1 | `queuedPlaceholderCmd("foo;rm -rf $HOME")` returns a string with no `;`, `$`, backticks, or newlines | `lib/claude-dispatcher.injection.test.ts` (new) |
| 2 | `lib/rate-limiter.ts` drops expired entries during use (Map size bounded under continuous unique keys + advancing time) | `lib/rate-limiter.test.ts` (extend existing or new) |
| 3 | Knowledge page helper handles malformed tags JSON without throwing | extend existing knowledge tests or add a small helper test |
| 4 | `lib/anthropic-feature-check.ts` parser surface returns safe default on non-JSON LLM output | `lib/anthropic-feature-check.test.ts` (existing) — add one case |
| 5 | `lib/deploy-monitor.ts` returns gracefully when fetch times out | `lib/deploy-monitor.test.ts` (new if absent; otherwise extend) |
| 6 | Prisma schema parses + indexes are present | implicit via `prisma generate` succeeding + grep |

The Overseer-chat client AbortController has no good unit test (would need DOM + interrupted SSE simulation). Validated by code review + tsc/lint.

## Out of scope

- Drift reconciliation — Phase 32.
- Route-boundary tests — Phase 33.
- Other audit items below MEDIUM severity.
